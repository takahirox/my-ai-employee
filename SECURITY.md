# Security policy

Version 0.1 receives security fixes. Report vulnerabilities privately to the project
maintainers rather than opening a public issue. Never include live credentials or
sensitive user data in a report. The runtime disables unrestricted network and process
authority by default; policy inputs may only add restrictions.

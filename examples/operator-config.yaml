schema_version: 1
workers:
  codex_cli:
    executable: /absolute/path/to/codex
    # Add interpreter/runtime directories only when the executable needs them.
    path_entries: [/absolute/path/to/runtime/bin]
  claude_code_cli:
    executable: /absolute/path/to/claude
  ollama_cli:
    executable: /absolute/path/to/ollama
routing:
  default_strategy_set: codex-balanced
  default_assessment_strategy: codex-sol-high
  # Sets reference strategies below; they cannot redefine a model or effort.
  strategy_sets:
    codex-balanced: [codex-luna-max, codex-sol-high]
    claude-only: [claude-opus-high, claude-fable-high]
    codex-claude: [codex-luna-max, codex-sol-high, claude-opus-high, claude-fable-high]
    local-only: [ollama-local]
  strategy_set_assessors:
    codex-balanced: codex-sol-high
    claude-only: claude-fable-high
  strategies:
    - id: codex-luna-max
      backend: codex_cli
      model: gpt-5.6-luna
      effort: max
      capabilities: [edit_intent, process]
      planner_eligible: true
      min_complexity: 1
      max_complexity: 3
      min_scale: 1
      max_scale: 3
      max_risk: 0
    - id: codex-sol-high
      backend: codex_cli
      model: gpt-5.6-sol
      effort: high
      capabilities: [edit_intent, process]
      planner_eligible: true
      min_complexity: 1
      max_complexity: 10
      min_scale: 1
      max_scale: 10
      max_risk: 10
    - id: claude-opus-high
      backend: claude_code_cli
      model: claude-opus-5
      effort: high
      capabilities: [edit_intent, process]
      planner_eligible: true
      min_complexity: 1
      max_complexity: 3
      min_scale: 1
      max_scale: 3
      max_risk: 0
    - id: claude-fable-high
      backend: claude_code_cli
      model: claude-fable-5
      effort: high
      capabilities: [edit_intent, process]
      planner_eligible: true
      min_complexity: 1
      max_complexity: 10
      min_scale: 1
      max_scale: 10
      max_risk: 10
    - id: ollama-local
      backend: ollama_cli
      # Replace with an installed, pinned Ollama model reference.
      model: REPLACE_WITH_PINNED_OLLAMA_MODEL
      effort: low
      capabilities: [edit_intent, process]
      min_complexity: 1
      max_complexity: 10
      min_scale: 1
      max_scale: 10
      max_risk: 10
